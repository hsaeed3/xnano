"""xnano.beta

Provides from the ``v1.0.0`` release, the ``xnano.beta`` namespace will contain
the new webui API as well as a command-line interface for defining and parsing
CLI commands.

``xnano.beta`` will remain the only export location for these features until the ``v1.1.0`` release,
in which the webui and CLI components will be moved directly into the ``xnano`` namespace.
"""

raise NotImplementedError(
    "As per version ``1.0.0`` the ``xnano.beta`` API has not yet been implemented with any submodules "
    "or functionality. These features are currently under active development and will be released very "
    "soon."
)
